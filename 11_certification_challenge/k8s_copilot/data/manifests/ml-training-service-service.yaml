apiVersion: v1
kind: Service
metadata:
  labels:
    app: ml-training
  name: ml-training-service
  namespace: ml
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: ml-training
  type: ClusterIP
