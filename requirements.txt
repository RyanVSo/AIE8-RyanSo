# Ultra-lightweight Vercel deployment requirements
# Minimal dependencies to stay under 250MB limit

# Core web framework
fastapi>=0.104.0

# Essential LangChain for OpenAI only
langchain-core>=0.3.0
langchain-openai>=0.2.0

# Minimal utilities
pydantic>=2.0.0
python-dotenv>=1.0.0
mangum>=0.17.0

# Removed heavy dependencies that cause size limit issues:
# - langchain-community (~100MB+ with dependencies)
# - langgraph (~50MB+ agent framework)  
# - qdrant-client (~30MB+ vector database)
# - pandas/numpy (~50MB+ data processing)
# - langchain-text-splitters (~20MB)
# - langchain-cohere (~30MB)
# - tiktoken (~20MB)
# - uvicorn (not needed for Vercel serverless)